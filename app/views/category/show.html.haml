= render "items/header"
.item-wrapper
  .title-box
    #{@categories.name} の商品一覧
  .content-box
    - @categories.children.each do |category|
      .contents
        = link_to category.name, category_path(category.id), class: "category-link"
= render "items/footer"
= render "items/sell_btn"


